
Commands beginning with a "$" are executed as a standard user. Commands beginnning with a "#" have to be executed as root.


Help can be shown with:
$ make help

To install Bash Simple Curses, run
$ make install

Then, if no error appeared, run the same command as root:
# make install

To uninstall:
# make uninstall
