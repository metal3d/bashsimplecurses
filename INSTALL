Installation:

To install Bash Simple Curses, run
$ sudo make install

Or to install it as a standard user
$ make install PREFIX=~/.local/lib


Uninstall:

You can uninstall the same way:
$ sudo make uninstall

As a standard user:
$ make uninstall PREFIX=./local/lib
